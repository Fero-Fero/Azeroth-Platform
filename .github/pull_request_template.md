## Summary

<!-- Why this change exists. One or two sentences; the commits can list the files. -->

## Related issue

<!-- Fixes #123 or N/A -->

## Test plan

- [ ] `dotnet test backend/AzerothPlatform.sln` (if backend touched)
- [ ] `dotnet test launcher/AzerothPlatform.Launcher.sln` (if launcher touched)
- [ ] `npm run lint && npm run test` in `frontend/` (if frontend touched)
- [ ] Manual check of the user-visible flow (and other pages that share the same state, if UI)

## Docs

- [ ] User-facing or API behaviour is unchanged, **or** README / CONTRIBUTING / comments were updated

## Notes

<!-- Anything a reviewer cannot see from the diff: leftover follow-ups, test gaps, deploy order. Do not put this in code comments. -->
